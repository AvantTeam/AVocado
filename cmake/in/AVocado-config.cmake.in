@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(NOT TARGET SDL2::SDL2)
    find_dependency(SDL2 REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/AVocado-targets.cmake")
check_required_components(AVocado avcore)
